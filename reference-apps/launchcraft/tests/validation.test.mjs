import assert from "node:assert/strict";import test from "node:test";import {slugify,validateAuth,validateProject,validateTask} from "../lib/validation.ts";
const form=(entries)=>{const data=new FormData();for(const[key,value]of Object.entries(entries))data.set(key,value);return data};
test("les identifiants de projet sont normalisés de façon déterministe",()=>{assert.equal(slugify("  Formation Été 2027 ! "),"formation-ete-2027")});
test("authentification et projets refusent les champs invalides",()=>{assert.equal(validateAuth(form({email:"x",password:"court",first_name:"A"}),true).success,false);assert.equal(validateProject(form({title:"ok",description:"",status:"active",color:"violet",start_date:"",due_date:""})).success,false)});
test("un projet valide ne contient jamais de user_id navigateur",()=>{const result=validateProject(form({title:"Mon lancement",description:"Description",status:"planning",color:"mint",start_date:"2026-09-01",due_date:"2026-10-01",user_id:"attaque"}));assert.equal(result.success,true);if(result.success)assert.deepEqual(Object.keys(result.data).sort(),["color","description","due_date","slug","start_date","status","title"])});
test("les tâches appliquent les statuts, priorités et dates stricts",()=>{assert.equal(validateTask(form({title:"Préparer la page",description:"",status:"completed",priority:"high",due_date:"2026-09-01"})).success,true);assert.equal(validateTask(form({title:"Préparer la page",description:"",status:"done",priority:"admin",due_date:"demain"})).success,false)});
