import assert from "node:assert/strict";import test from "node:test";import {calculateDashboardStats,calculateProgress,getUpcomingTasks,isTaskOverdue} from "../lib/metrics.ts";
const projects=[{id:"p1",status:"active"},{id:"p2",status:"planning"},{id:"p3",status:"completed"}];const tasks=[{id:"t1",projectId:"p1",status:"completed",dueDate:"2026-08-10"},{id:"t2",projectId:"p1",status:"todo",dueDate:"2026-08-20"},{id:"t3",projectId:"p2",status:"in_progress",dueDate:"2026-08-25"},{id:"t4",projectId:"p2",status:"completed",dueDate:null}];
test("la progression vient uniquement des tâches",()=>{assert.equal(calculateProgress("p1",tasks),50);assert.equal(calculateProgress("p2",tasks),50);assert.equal(calculateProgress("p3",tasks),null)});test("une tâche terminée ou sans date n’est jamais en retard",()=>{assert.equal(isTaskOverdue(tasks[0],"2026-08-21"),false);assert.equal(isTaskOverdue(tasks[1],"2026-08-21"),true);assert.equal(isTaskOverdue(tasks[3],"2026-08-21"),false)});test("les statistiques sont toutes calculées",()=>{assert.deepEqual(calculateDashboardStats(projects,tasks,"2026-08-21"),{activeProjects:2,completedTasks:2,remainingTasks:2,overdueTasks:1,averageProgress:50})});test("les prochaines échéances excluent retard et tâches terminées",()=>{assert.deepEqual(getUpcomingTasks(tasks,"2026-08-21").map(({id})=>id),["t3"])});
