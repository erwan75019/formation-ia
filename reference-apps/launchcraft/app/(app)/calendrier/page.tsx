import Calendar from "@/components/Calendar";import PageHeader from "@/components/PageHeader";import UpcomingTasks from "@/components/UpcomingTasks";import {getWorkspace} from "@/lib/data";import {getUpcomingTasks} from "@/lib/metrics";
export default async function CalendarPage(){const{projects,tasks}=await getWorkspace();const today=new Date().toISOString().slice(0,10);return <main id="main-content" tabIndex={-1}><PageHeader eyebrow="PLANNING" title="Calendrier" description="Toutes les échéances sont calculées depuis vos tâches."/><div className="calendar-page"><Calendar tasks={tasks} projects={projects}/><UpcomingTasks tasks={getUpcomingTasks(tasks,today,10)} projects={projects}/></div></main>}
