import PageHeader from "@/components/PageHeader";import ProfileForm from "@/components/ProfileForm";import {getWorkspace} from "@/lib/data";
export default async function SettingsPage(){const{firstName,user}=await getWorkspace();return <main id="main-content" tabIndex={-1}><PageHeader eyebrow="COMPTE" title="Paramètres du profil" description="Mettez à jour les informations visibles dans votre espace."/><section className="settings panel"><div className="avatar-large">{firstName.slice(0,2).toUpperCase()||"LC"}</div><ProfileForm firstName={firstName} email={user.email??""}/></section></main>}
